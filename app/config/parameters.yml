# This file is auto-generated during the composer install
parameters:
    graviton.log.path: '%env(resolve:LOG_PATH)%'
    graviton.log.level: '%env(resolve:LOG_LEVEL)%'
    graviton.sentry.dontReport: '%env(resolve:SENTRY_DONT_REPORT)%'
    graviton.core.checkPackageName: '%env(resolve:CHECK_PACKAGE_NAME)%'
    graviton.mongodb.default.server.db: '%env(resolve:MONGODB_DB)%'
    graviton.mongodb.default.server.uri: '%env(resolve:MONGODB_URI)%'
    graviton.rest.pagination.limit: '%env(resolve:PAGINATION_LIMIT)%'
    graviton.core.httpclient.debugRequests: '%env(resolve:HTTPCLIENT_DEBUGREQUESTS)%'
    graviton.core.httpclient.debugMaxLength: '%env(resolve:HTTPCLIENT_DEBUGREQUESTS_MAXLENGTH)%'
    graviton.core.httpclient.verifyPeer: '%env(resolve:HTTPCLIENT_VERIFY_PEER)%'
    graviton.proxy: '%env(resolve:PROXY)%'
    graviton.noproxy: '%env(resolve:NOPROXY)%'
    graviton.composer.cmd: '%env(resolve:COMPOSER_CMD)%'
    graviton.rabbitmq.queueevent.transient_headers: '%env(resolve:RABBITMQ_QUEUEEVENT_TRANSIENTHEADERS)%'
    graviton.rabbitmq.host: '%env(resolve:RABBITMQ_HOST)%'
    graviton.rabbitmq.port: '%env(resolve:RABBITMQ_PORT)%'
    graviton.rabbitmq.user: '%env(resolve:RABBITMQ_USER)%'
    graviton.rabbitmq.password: '%env(resolve:RABBITMQ_PASSWORD)%'
    graviton.rabbitmq.vhost: '%env(resolve:RABBITMQ_VHOST)%'
    graviton.proxy.swagger.sources: '%env(resolve:PROXY_SWAGGER_SOURCES)%'
    graviton.security.authentication.allow_anonymous: '%env(resolve:SECURITY_AUTHENTICATION_ALLOW_ANONYMOUS)%'
    graviton.security.authentication.test_username: '%env(resolve:SECURITY_AUTHENTICATION_TEST_USERNAME)%'
    graviton.security.authentication.strategy.header.key: '%env(resolve:SECURITY_AUTHENTICATION_STRATEGY_HEADER_KEY)%'
    graviton.security.authentication.strategy.cookie.key: '%env(resolve:SECURITY_AUTHENTICATION_STRATEGY_COOKIE_KEY)%'
    graviton.security.authentication.strategy.subnet.key: '%env(resolve:SECURITY_AUTHENTICATION_STRATEGY_SUBNET_KEY)%'
    graviton.security.authentication.strategy.subnet.header_field: '%env(resolve:SECURITY_AUTHENTICATION_STRATEGY_SUBNET_HEADER_FIELD)%'
    graviton.security.authentication.provider.model: '%env(resolve:SECURITY_AUTHENTICATION_PROVIDER_MODEL)%'
    graviton.security.authentication.provider.model.query_field: '%env(resolve:SECURITY_AUTHENTICATION_MODEL_QUERY_FIELD)%'
    graviton.cache.instance_id: '%env(resolve:CACHE_INSTANCE_ID)%'
    graviton.cache.redis.host: '%env(resolve:CACHE_REDIS_HOST)%'
    graviton.cache.redis.port: '%env(resolve:CACHE_REDIS_PORT)%'
    graviton.cache.server.hostname: '%env(resolve:CACHE_SERVER_HOSTNAME)%'
    graviton.file.backend: '%env(resolve:FILE_BACKEND)%'
    graviton.file.local.storage_directory: '%env(resolve:FILE_LOCAL_STORAGE_DIRECTORY)%'
    graviton.file.allowed.mime_types: '%env(resolve:FILE_ALLOWED_MIME_TYPES)%'
    graviton.file.read_filesystem_mimetype: '%env(resolve:FILE_READ_FS_MIMETYPE)%'
    graviton.file.s3.endpoint: '%env(resolve:FILE_S3_ENDPOINT)%'
    graviton.file.s3.version: '%env(resolve:FILE_S3_VERSION)%'
    graviton.file.s3.region: '%env(resolve:FILE_S3_REGION)%'
    graviton.file.s3.bucket: '%env(resolve:FILE_S3_BUCKET)%'
    graviton.file.s3.credentials.key: '%env(resolve:FILE_S3_KEY)%'
    graviton.file.s3.credentials.secret: '%env(resolve:FILE_S3_SECRET)%'
    graviton.rest.serializer.groups: '%env(resolve:SERIALIZER_GROUPS)%'
    graviton.rest.serializer.allow_group_override_header: '%env(resolve:SERIALIZER_ALLOW_GROUP_OVERRIDE_HEADER)%'
    graviton.rest.serializer.override_header_name: '%env(resolve:SERIALIZER_GROUP_OVERRIDE_HEADER_NAME)%'
    graviton.rest.schema_variation.enable: '%env(resolve:SCHEMA_VARIATION_ENABLED)%'
    graviton.rest.data_restriction.map: '%env(resolve:DATA_RESTRICTION_MAP)%'
    graviton.rest.data_restriction.mode: '%env(resolve:DATA_RESTRICTION_MODE)%'
    graviton.rest.data_restriction.persist: '%env(resolve:DATA_RESTRICTION_PERSIST)%'
    graviton.rest.data_restriction.solr: '%env(resolve:DATA_RESTRICTION_SOLR)%'
    graviton.rest.data_restriction.conditional.persist.map: '%env(resolve:DATA_RESTRICTION_CONDITIONAL_PERSIST_MAP)%'
    graviton.worker.relativeUrl: '%env(resolve:WORKER_RELATIVE_URL)%'
    graviton.analytics.cache_time.metadata: '%env(resolve:ANALYTICS_CACHE_TIME_METADATA)%'
    graviton.sentry.dsn: '%env(resolve:SENTRY_DSN)%'
    graviton.solr.url: '%env(resolve:SOLR_URL)%'
    graviton.solr.fuzzy_bridge: '%env(resolve:SOLR_FUZZY_BRIDGE)%'
    graviton.solr.wildcard_bridge: '%env(resolve:SOLR_WILDCARD_BRIDGE)%'
    graviton.solr.andify_terms: '%env(resolve:SOLR_ANDIFY_TERMS)%'
